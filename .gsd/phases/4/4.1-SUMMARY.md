# Plan 4.1 Summary: Firestore Data Layer

## Completed

- Added `subscribeToEmails(userId, callback)` to `src/api/db.js` — real-time `onSnapshot` listener with `where("userId")`, `orderBy("sentAt", "desc")`, `limit(50)`
- Added `subscribeToEvents(emailId, callback)` — real-time listener on events sub-collection ordered by `timestamp` desc
- Added `getEmailWithEvents(emailId)` — one-shot fetch of email + events sub-collection
- Created `firestore.indexes.json` with composite index on `userId ASC + sentAt DESC`
- Updated `firebase.json` to reference `firestore.indexes.json`

## Verification

- `npm run build` passes ✓
- Firestore rules reviewed — `allow read` covers both `get` and `list`, `where("userId", "==", userId)` satisfies rule evaluation ✓
