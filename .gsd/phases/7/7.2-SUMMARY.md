# Summary: Plan 7.2 — Click Drill-Down

## Status: COMPLETE

## What Was Done

- Extended `buildRecipientStats` to include `urlClicks: {}` map per recipient (keyed by `targetUrl`, value `{ count, first, last }`)
- Added `expandedRecipient` state to `EmailRow` to track which recipient's URL list is open
- Replaced `<Stat label="Clicks" .../>` with a clickable div showing `N ▾` indicator
- Clicking the Clicks cell toggles a per-URL breakdown below the stats grid
- Each URL row: truncated URL (`maxWidth: 260px`, `textOverflow: ellipsis`), `title` attribute for hover tooltip, count + first/last timestamps
- `e.stopPropagation()` prevents email row collapse when interacting with URL list

## Verification

- `npm run build` passes ✓
- `urlClicks` map in `buildRecipientStats` ✓
- Collapsible URL list with truncation and tooltip ✓
