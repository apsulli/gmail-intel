---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Typography and Dark Theme Variables

## Objective

Establish the pink/black dark theme variables and integrate local Google Fonts ("Fredoka" for headers, "Nunito" for body) to bypass Gmail's CSP.

## Context

- .gsd/ROADMAP.md (Phase 8)
- src/index.css
- .ai/assets/promotional_tile.png (Reference for pink accent color `#FF1493` DeepPink-like)

## Tasks

<task type="auto">
  <name>Download Local Fonts</name>
  <files>
    - src/fonts/Fredoka.woff2
    - src/fonts/Nunito.woff2
  </files>
  <action>
    - Create `src/fonts/` directory.
    - Download `Fredoka` and `Nunito` `.woff2` font files locally (or find a reliable script to fetch them within the executor). Alternatively, run an NPM install for a self-hosted font package like `@fontsource/fredoka` and `@fontsource/nunito` to let Vite handle it.
    - If using `@fontsource`, ensure it's added via `npm install` and imported in `index.jsx` / `index.css`.
  </action>
  <verify>Check `src/fonts` directory or `package.json` for fontsource</verify>
  <done>Fonts downloaded/installed successfully.</done>
</task>

<task type="auto">
  <name>Setup Dark Theme CSS Variables</name>
  <files>
    - src/index.css
  </files>
  <action>
    - Update `:root` or `#gmail-intel-sidebar` in `src/index.css` to define:
      - `--bg-sidebar: #121212`
      - `--bg-card: #202020`
      - `--text-main: #FFFFFF`
      - `--text-muted: #A0A0A0`
      - `--accent-primary: #FF1493` // Hot pink to match the promotional tile
      - `--accent-secondary: #00FFFF` // Cyan/neon blue for a fun contrast
      - `--font-header: 'Fredoka', sans-serif`
      - `--font-body: 'Nunito', sans-serif`
    - Add `@font-face` rules for the downloaded fonts, or import the fontsource CSS.
  </action>
  <verify>CSS variables and font rules added</verify>
  <done>CSS variables defined for dark theme and typography</done>
</task>
