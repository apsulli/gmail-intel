---
phase: 8
plan: 2
wave: 1
---

# Plan 8.2: App Header & Sidebar Toggle Emoji Update

## Objective

Replace existing emojis and icons with the "eyes" target (👀) and apply the new font to the headers.

## Context

- src/sidebar.js
- src/dashboard/DashboardApp.jsx
- src/index.css

## Tasks

<task type="auto">
  <name>Update Sidebar Toggle Icon</name>
  <files>
    - src/sidebar.js
  </files>
  <action>
    - Update the toggle button DOM creation logic in `sidebar.js`.
    - Change the internal visible text or icon injection to use the emoji `👀`.
    - Give it a fun, dark-themed background using `--bg-card` and maybe an `--accent-primary` border/ring to ensure it stands out but fits the theme.
  </action>
  <verify>Check `src/sidebar.js` content</verify>
  <done>Sidebar toggle displays 👀 and matches dark styling</done>
</task>

<task type="auto">
  <name>Update Dashboard Header</name>
  <files>
    - src/dashboard/DashboardApp.jsx
    - src/index.css
  </files>
  <action>
    - Update `DashboardApp.jsx` header to include `👀 Gmail Intel`.
    - Set `font-family: var(--font-header)` for the main `<h1/h2>` class representing the title.
    - Make the header text color `--text-main` or `--accent-primary`.
    - Restyle the close button (✕) and refresh button (↺) to match the dark theme, using `--accent-primary` coloring on hover.
  </action>
  <verify>Check component syntax and CSS</verify>
  <done>Dashboard header shows 👀, uses Fredoka, and feels dark-themed</done>
</task>
