---
phase: 8
plan: 3
wave: 2
---

# Plan 8.3: Apply Dark Theme to Dashboard Emails

## Objective

Flow the "pink and black" theme deeply into the dashboard, restyling email cards, recipient rows, pagination buttons, and headers.

## Context

- src/dashboard/DashboardApp.jsx
- src/index.css

## Tasks

<task type="auto">
  <name>Card & Weekly Header Styles</name>
  <files>
    - src/index.css
  </files>
  <action>
    - Set the sidebar's main container component background to `--bg-sidebar`.
    - Set the overall text color to `--text-main` and font-family to `--font-body`.
    - Restyle `.email-item` (or the respective email wrapper equivalent) to use `--bg-card` with an elegant border-color like `#333333` and some rounded corners (`border-radius: 12px`).
    - Change subtexts or timestamps to use `--text-muted`.
    - Style the `.week-header` to appear fun and readable: uppercase `var(--font-header)`, colored `--accent-primary`, and maybe sticky.
  </action>
  <verify>CSS contains updated background and text properties connected to variables</verify>
  <done>Email list looks cohesive with the dark mode card UI</done>
</task>

<task type="auto">
  <name>Expanded Details & Pagination Accents</name>
  <files>
    - src/index.css
    - src/dashboard/DashboardApp.jsx
  </files>
  <action>
    - Restyle the expanded recipient stats and clicked URLs inside the dashboard. Use slightly darker or lighter backgrounds (`#2A2A2A`).
    - Use `--accent-secondary` (cyan) for hover states on URL links inside the expanded detail for interactive contrast against the pink theme.
    - Give the "Load more" button a cute pill shape (`border-radius: 999px`) with a background of `--bg-card` and a solid box-shadow/border of `--accent-primary`, styled with the bubble font.
  </action>
  <verify>Check CSS for pagination and stats hover classes</verify>
  <done>All interior components and buttons utilize the new dark theme and fonts fully</done>
</task>
