# Plan 5.3 Summary — Per-Recipient Analytics

## What Was Done

Replaced the flat recipient address list in the expanded email row with a per-recipient
stats grid showing opens, clicks, last open timestamp, and last click timestamp.

### Changes Made

**`src/dashboard/DashboardApp.jsx`**
- Added `Stat({ label, value })` component — renders a labelled stat cell with `—` fallback
- Added `formatTs(date)` — formats Date as "Xm ago", "Xh ago", or "Mon D, HH:MM"
- Added `buildRecipientStats(recipients, events)` — correlates events by recipientId,
  computes opens/clicks/lastOpen/lastClick per recipient
- Replaced flat `{r.email}` list in `EmailRow` expanded section with an IIFE that
  renders a 2-column grid (`Opens | Clicks | Last open | Last click`) per recipient
- `Stat` shows `'—'` when value is falsy (0 or null)

## Verification Checklist

- [x] Build passes
- [ ] Manual: expand tracked email → each recipient shows stats grid
- [ ] Manual: opens/clicks counts match Firestore events
- [ ] Manual: timestamps show relative format for recent, date for older
- [ ] Manual: unopened recipients show — for last open/click
- [ ] Manual: counts update live when email opened in another window
