# Technology Stack

> Auto-generated by /map on 2026-03-02

## Runtime

| Technology           | Version | Purpose                 |
| -------------------- | ------- | ----------------------- |
| Node.js              | 22      | Cloud Functions runtime |
| Chrome (Manifest V3) | —       | Extension runtime       |

## Dependencies

### Production (Extension)

| Package     | Version (pinned) | Version (installed) | Purpose                            |
| ----------- | ---------------- | ------------------- | ---------------------------------- |
| `firebase`  | ^10.8.0          | 10.14.1             | Firebase Web SDK (Auth, Firestore) |
| `react`     | ^18.2.0          | 18.3.1              | UI framework for popup dashboard   |
| `react-dom` | ^18.2.0          | 18.3.1              | React DOM renderer                 |

### Production (Cloud Functions)

| Package              | Version (pinned) | Purpose                        |
| -------------------- | ---------------- | ------------------------------ |
| `firebase-admin`     | ^13.7.0          | Admin SDK for Firestore writes |
| `firebase-functions` | ^7.0.6           | Cloud Functions framework      |

### Development

| Package                | Version (pinned) | Version (installed) | Purpose                           |
| ---------------------- | ---------------- | ------------------- | --------------------------------- |
| `@crxjs/vite-plugin`   | ^2.0.0-beta.23   | 2.0.0-beta.23       | Chrome extension Vite integration |
| `@vitejs/plugin-react` | ^4.2.1           | 4.7.0               | React fast-refresh for Vite       |
| `vite`                 | ^5.1.4           | 5.4.21              | Build tool and dev server         |

## Chrome Extension APIs Used

| API                | Permission  | Purpose                                                  |
| ------------------ | ----------- | -------------------------------------------------------- |
| `chrome.identity`  | `identity`  | OAuth token retrieval via `getAuthToken()`               |
| `chrome.runtime`   | —           | Message passing between content script and background SW |
| `chrome.scripting` | `scripting` | Content script management                                |
| `chrome.storage`   | `storage`   | Local storage (declared but not actively used)           |

## Infrastructure

| Service          | Provider       | Purpose                                                  |
| ---------------- | -------------- | -------------------------------------------------------- |
| Firestore        | Firebase (GCP) | Document database for email metadata and tracking events |
| Cloud Functions  | Firebase (GCP) | HTTP endpoints for pixel + click tracking                |
| Firebase Auth    | Firebase (GCP) | Google sign-in for multi-tenant user isolation           |
| Firebase Hosting | Firebase (GCP) | Landing page and privacy policy hosting                  |
| Gmail API        | Google Cloud   | Sending emails on behalf of users                        |
| OAuth 2.0        | Google Cloud   | User authentication and API access                       |

## Configuration

| Variable / Key   | Purpose                                      | Location                                  |
| ---------------- | -------------------------------------------- | ----------------------------------------- |
| Firebase config  | App initialization (apiKey, projectId, etc.) | `src/firebase-config.js`                  |
| OAuth Client ID  | Chrome identity API client                   | `manifest.json` → `oauth2.client_id`      |
| `PIXEL_URL`      | Tracking pixel endpoint                      | `src/content.js` (hardcoded)              |
| `CLICK_URL`      | Click tracking endpoint                      | `src/content.js` (hardcoded)              |
| Firebase project | `gm-intel`                                   | `firebase.json`, `src/firebase-config.js` |

## Outdated Packages

### Extension (`package.json`)

| Package                | Current | Latest  | Risk                                      |
| ---------------------- | ------- | ------- | ----------------------------------------- |
| `firebase`             | 10.14.1 | 12.10.0 | ⚠️ Major — may require migration          |
| `react`                | 18.3.1  | 19.2.4  | ⚠️ Major — React 19 has breaking changes  |
| `react-dom`            | 18.3.1  | 19.2.4  | ⚠️ Major — must match React version       |
| `@vitejs/plugin-react` | 4.7.0   | 5.1.4   | ⚠️ Major — may require Vite 6+            |
| `vite`                 | 5.4.21  | 7.3.1   | ⚠️ Major — multiple major versions behind |

### Cloud Functions (`functions/package.json`)

| Package              | Current | Latest       | Risk   |
| -------------------- | ------- | ------------ | ------ |
| `firebase-admin`     | ^13.7.0 | (up to date) | ✅ Low |
| `firebase-functions` | ^7.0.6  | (up to date) | ✅ Low |

## Build & Deployment

| Task             | Command                            | Notes                     |
| ---------------- | ---------------------------------- | ------------------------- |
| Dev server       | `npm run dev`                      | Vite dev mode with HMR    |
| Production build | `npm run build`                    | Outputs to `dist/`        |
| Deploy functions | `firebase deploy --only functions` | From project root         |
| Deploy hosting   | `firebase deploy --only hosting`   | Serves `hosting/` dir     |
| Deploy rules     | `firebase deploy --only firestore` | Deploys `firestore.rules` |
| Preview build    | `npm run serve`                    | Vite preview mode         |

## Project Structure

```
gmail-intel/
├── manifest.json          # Chrome Extension manifest (MV3)
├── index.html             # Popup entry point (React)
├── package.json           # Extension dependencies
├── vite.config.js         # Vite + CRXJS config
├── firebase.json          # Firebase project config
├── firestore.rules        # Firestore security rules
├── bootstrap.md           # Original build instructions
├── src/
│   ├── main.jsx           # React app entry
│   ├── App.jsx            # Popup dashboard (stub)
│   ├── content.js         # Gmail content script (core logic)
│   ├── background.js      # Service worker (auth + API proxy)
│   ├── firebase-config.js # Firebase project config
│   └── api/
│       ├── gmail.js       # Gmail API email sending
│       └── db.js          # Firestore write operations
├── functions/
│   ├── package.json       # Cloud Functions dependencies
│   └── index.js           # trackPixel + trackClick functions
├── hosting/
│   ├── index.html         # Landing page
│   └── privacy.html       # Privacy policy
└── dist/                  # Built extension output
```
